---
name: pr
description: Create a PR.
---

Create a PR, title format follows project's commit rules, sensible branch name. Body is 1-3 (default 2) paragraphs depending on size. Keep concise, explain what changed/added/fixed, not the granular details. 

Do not attribute Claude.

Mention `# Tests` without the technicalities; high level only.