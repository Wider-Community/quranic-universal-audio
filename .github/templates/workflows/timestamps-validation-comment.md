<!-- timestamps-validation -->
## {header_glyph} Timestamps validation — {reciter}

First timestamps push for this reciter.

**Run config**
- Audio category: `{audio_category}`
- Aligner model: `{aligner_model}`

**Coverage**
| Metric | Value |
| --- | --- |
| Verses present | {verses_present} / {total_verses} |
| Missing verses | {missing_verses} |
| Word-coverage issues | {word_coverage_issues} |
| Forward-gap verses | {forward_gap_verses} |
| Zero-duration words | {zero_duration_words} |

**MFA alignment failures: {mfa_failures_count}**
{mfa_failures_block}
**Files**
- `{ts_path}`
- `{ts_full_path}`

_Posted by `timestamps-first-push-comment.yml`._
