#!/usr/bin/env sh
# Intentionally a no-op. CI is the ground-truth gate for pushes:
# inspector-deploy.yml + docker-publish.yml run the full lint/typecheck/test/
# build via the reusable inspector-checks.yml workflow. Keeping push instant
# avoids local friction; the staged-only pre-commit hook is the cheap local pass.
exit 0
