#!/usr/bin/env sh
# Opt-in: default skip. Run with `VERIFY=1 git commit` (or alias `git cv`).
[ "${VERIFY:-0}" = "1" ] || exit 0
# -p false: serial task chunks; parallel ESLint hit OOM on large stage sets.
exec node ./node_modules/lint-staged/bin/lint-staged.js -p false
