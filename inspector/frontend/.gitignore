node_modules/
dist/
.vite/
*.log
playwright-report/
test-results/
blob-report/
.playwright/
vite.config.ts.timestamp-*
.ruff_cache/

# Paraglide JS codegen output — regenerated by the Vite plugin / paraglide:compile
# on every build & dev start (unlike the committed Pydantic→TS schemas codegen).
# The compiler also emits its own .gitignore inside this dir; this is belt-and-braces.
src/lib/paraglide/