const source = `
# Missing Verses

A missing verse is a whole verse with no segment at all. The cause might be a genuine omission, an audio problem, a knock-on effect of missing words, or an alignment failure that didn't detect verse text that's actually present.

If the verse was recited, it's in the audio — an edit just has to find it. By the end this should be zero, unless a verse is truly absent from the recording.
`;

export default source;
