const source = `
# Qalqala

Information only. Qalqala letters are almost always fine — segments are padded automatically so the echo isn't cut off and mistaken for silence, so usually there's nothing to do.

The exception is slow or mujawwad recitation, or when there are a few failed alignmnets due to qalqala, or certain letters like qaf ق, where a long gap before the echo can leave it clipped. The waveform shape makes it obvious — you can skim and glance through the waveforms and act if the qalqala is genuinely cut.

::example{id="qalqala_extend"}
`;

export default source;
