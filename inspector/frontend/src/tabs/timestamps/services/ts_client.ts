/**
 * Timestamps tab — client-side shard-fetch data layer.
 *
 * Re-export barrel for the shared shard-fetch implementation at
 * `lib/recitation-data/ts-source.ts`, keeping the
 * `./services/ts_client` import path stable for the timestamps tab and its tests.
 */

export * from '../../../lib/recitation-data/ts-source';
