# Development-only dependencies for the inspector.
#
# Production Docker image stays lean — the Dockerfile only `pip install`s
# requirements.txt. These extras are local / CI only.
#
# Install locally with:
#   pip install -r inspector/requirements.txt -r inspector/requirements-dev.txt

pytest>=8
pytest-cov>=5

# Schema → TypeScript codegen for the FE.
# Used by scripts/codegen/regen_fe_types.py to keep
# inspector/frontend/src/lib/types/generated/schemas.ts in lockstep with
# the Pydantic schemas at qua_shared/schemas/. CI verifies the generated
# file is in sync.
pydantic-to-typescript>=2.0,<3
